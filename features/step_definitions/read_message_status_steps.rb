Given(/^the user returns to the message index page$/) do
  visit messages_path
end

Then(/^the read message should show as read$/) do
  pending # express the regexp above with the code you wish you had
end
